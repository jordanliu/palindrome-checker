language: node_js
node_js: 
  - "stable"
script:
  - yarn test
  - yarn build
  - yarn run deploy

deploy:
  provider: pages
  skip-cleanup: true
  github-token: GITHUB_TOKEN
  local_dir: build
  on:
    branch: master